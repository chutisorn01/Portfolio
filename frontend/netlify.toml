# Build settings
[build]
  command = "npm run build"
  publish = "dist"
  
# Redirect rules for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (optional - add in Netlify dashboard)
# NODE_VERSION = "18"
